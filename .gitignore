# General
.DS_Store
.env

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/cli

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# IDE
/.vscode
/.idea

# NPM
node_modules
package-lock.json

# Initialized by cli for local testing
gentabase
# Exception: the npm wrapper's stub binary must be tracked so it ships in
# the published tarball (postinstall replaces it with the downloaded binary).
!npm/bin/gentabase
